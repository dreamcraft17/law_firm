# Base URL backend API. Kalau backend = app ini (Next.js), set ke URL app + /api.
# Contoh production: https://xxx.vercel.app/api | lokal: http://localhost:3000/api
# Kosongkan = panel akan pakai same-origin /api.
NEXT_PUBLIC_API_BASE_URL=

# Wajib untuk backend (Next.js API + Prisma). Di Vercel HARUS pakai Public URL dari Railway
# (bukan postgres.railway.internal). Railway → PostgreSQL → Connect → Public URL.
# Jika di Vercel DATABASE_URL masih internal, set DATABASE_PUBLIC_URL = URL publik; kode akan pakai itu.
DATABASE_URL=postgresql://postgres:JaJpcGMJjDBIcWNbymRpzWYZgPPIaUBv@hopper.proxy.rlwy.net:29883/railway
