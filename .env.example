# Base URL backend API. Kalau backend = app ini (Next.js), set ke URL app + /api.
# Contoh production: https://xxx.vercel.app/api | lokal: http://localhost:3000/api
# Kosongkan = panel akan pakai same-origin /api.
NEXT_PUBLIC_API_BASE_URL=

# Wajib untuk backend (Next.js API + Prisma). Juga untuk npm run db:migrate (pakai Public URL dari Railway).
DATABASE_URL=postgresql://user:password@host:5432/railway
