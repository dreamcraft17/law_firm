# Base URL backend API (dipakai oleh admin-web untuk fetch data).
# Di production (Vercel): set di Environment Variables.
NEXT_PUBLIC_API_BASE_URL=https://api.example-firm.com

# DATABASE_URL dipakai oleh BACKEND (bukan oleh admin-web).
# Kalau backend jalan di Railway, set DATABASE_URL di Railway dari service PostgreSQL.
# Contoh: postgresql://user:pass@host.railway.app:5432/railway
# DATABASE_URL=
